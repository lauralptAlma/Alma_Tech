{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "flake8 dentalE && bandit -r dentalE && python manage.py collectstatic",
        "test": "python manage.py test dentalE"
      }
    }
  }
}
